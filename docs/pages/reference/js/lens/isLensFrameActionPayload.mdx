# isLensFrameActionPayload

Returns true if the given frame action payload is a Lens frame action payload.

## Usage

```ts

import { isLensFrameActionPayload } from 'frames.js/lens';

...

if (isLensFrameActionPayload(payload)) {
  // payload is a Lens frame action payload
}

```
