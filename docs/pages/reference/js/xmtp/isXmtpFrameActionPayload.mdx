# isXmtpFrameActionPayload

Returns true if the given frame action payload is an XMTP frame action payload.

## Usage

```ts

import { isXmtpFrameActionPayload } from 'frames.js/xmtp';

...

if (isXmtpFrameActionPayload(payload)) {
  // payload is an XMTP frame action payload
}

```
