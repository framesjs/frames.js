# getUsernameForFid

Returns the latest username for a given Farcaster user's `fid` if available, otherwise returning `null`.

## Usage

```ts [example.ts]
import { getUsernameForFid } from "frames.js"

const username = await getUsernameForFid({
	fid: 1,
});

console.log(username) // farcaster
```
